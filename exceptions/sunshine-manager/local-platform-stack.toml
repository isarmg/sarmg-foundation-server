id = "local-platform-stack"
product = "sunshine-manager"
reason = "Sunshine 是首个参考消费者，等待 Admin、Schema、Runtime 与 Operation Engine 纵向迁移"
introduced_at = "0.3.1"
must_remove_before = "1.0.0"
security_reduction = false
rules = ["foundation-route-ownership", "platform-schema-ownership", "platform-policy-ownership", "generated-schema"]
