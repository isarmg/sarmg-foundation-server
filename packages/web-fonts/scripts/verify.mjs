import{readFile}from"node:fs/promises";import{createHash}from"node:crypto";const provenance=JSON.parse(await readFile(new URL("../provenance.json",import.meta.url),"utf8"));for(const[name,expected]of Object.entries(provenance.assets)){const bytes=await readFile(new URL(`../${name}`,import.meta.url));const actual=createHash("sha256").update(bytes).digest("hex");if(actual!==expected)throw new Error(`${name} provenance mismatch`)}
