import react from "@vitejs/plugin-react";import{defineConfig,type UserConfig}from"vite";export function createSarmgReactViteConfig(options:{base?:string;maxAssetBytes?:number}={}):UserConfig{return defineConfig({...(options.base===undefined?{}:{base:options.base}),plugins:[react()],build:{outDir:"dist",emptyOutDir:true,sourcemap:false,chunkSizeWarningLimit:Math.floor((options.maxAssetBytes??512*1024)/1024)}})}
