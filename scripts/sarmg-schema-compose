#!/usr/bin/env python3
"""Repository launcher for the deterministic schema composer."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "tools"))

from sarmg_schema_compose import main  # noqa: E402

raise SystemExit(main())
