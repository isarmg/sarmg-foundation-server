{
  "name": "sarmg-foundation",
  "version": "0.5.0",
  "private": true,
  "description": "Sarmg 产品的构建期上游平台规范、基础实现与一致性工具工作区",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/isarmg/sarmg-foundation.git"
  },
  "engines": {
    "node": ">=26.7.0 <27"
  },
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "pnpm -r build",
    "package:check": "python3 scripts/package-artifacts.py check",
    "package:release": "python3 scripts/package-artifacts.py release --output release/npm",
    "package:smoke": "python3 scripts/package-artifacts.py smoke",
    "typecheck": "pnpm -r build && pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
