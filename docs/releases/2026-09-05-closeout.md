# 2026-09-05 多仓库阶段收尾

范围：已有改造工作的提交、不可变依赖落地、真实 CI/发行验收及 GitHub 发布。不是《更新手册》全部完成声明，也不包括旧版本升级或兼容代码。

## 架构与提交

- 原 Foundation 的 GitHub 仓库更名为 `sarmg-foundation-server`，保留原 Git 历史；独立创建 `sarmg-foundation-client` 仓库和历史。两库互不依赖。
- Server 管理 Web 归 Server；Client 本机管理 Web 归 Client/client。Host Windows 托盘本地页面没有迁入 Server。五个产品的主 `clients/web` 管理 Server。
- 六个消费者的 Server Rust 依赖锁定 `0.6.0` 和完整提交 `1e889d08fa69fcf2b5fffe45e8cc42b68218f4f1`；Web 使用 GitHub v0.6.0 tarball 和 lockfile integrity，不使用同级源码路径。
- Host/Media 的 Client Rust 依赖锁定 `0.6.0` 和完整提交 `a5b3158aa62af8189dd909500758cee649898e5c`。该源码版本后来确认 Windows Spool 阻断，见下表。
- 本次修复了发行指纹、当前 readiness、字体归档校验、原生测试夹具、独立构建缺失 Web 资源和特殊字符路径等真实失败。没有关闭安全门禁、降低覆盖率要求、跳过失败测试或改写已发布标签。
- `consumers/repositories.toml` 和生成矩阵记录本轮真实提交及直接使用的 Server 包。既有 `local-platform-stack` 例外仍保留为未完成项，不能解释为全部符合手册。旧的 baseline 记录不是新产品当前 Schema 的验收证据；Upgrade 当前 Schema 适配尚未完成，矩阵改记 `migration-in-progress`。

## 发布与验收状态

| 仓库 | 版本 | 结果与证据 |
|---|---|---|
| sarmg-foundation-server | 0.6.0 | [正式发布](https://github.com/isarmg/sarmg-foundation-server/releases/tag/v0.6.0)；[CI](https://github.com/isarmg/sarmg-foundation-server/actions/runs/33976063385) 与 [Release](https://github.com/isarmg/sarmg-foundation-server/actions/runs/33976223186) 成功；8 个 Web 包、发布工具及清单/摘要 |
| sarmg-foundation-client | 0.6.0 | [预发布源码](https://github.com/isarmg/sarmg-foundation-client/releases/tag/v0.6.0)；Linux Rust/Python/优化 HTTP/DNS 验收通过，Windows Spool 确认失败，不能作为已验收的 Windows SDK |
| sunshine-manager | 0.8.0 | [正式发布](https://github.com/isarmg/sunshine-manager/releases/tag/v0.8.0)；[最终 CI](https://github.com/isarmg/sunshine-manager/actions/runs/33978639141) 成功；真实归档、重定位启动、readiness、静态资源和篡改拒绝通过，附校验和及字体许可证 |
| sentinel-monitor | 0.2.1 | [正式发布](https://github.com/isarmg/sentinel-monitor/releases/tag/v0.2.1)；[最终 CI](https://github.com/isarmg/sentinel-monitor/actions/runs/33982923287) 全部成功；显式关闭未使用的 RTMP/SRT/MoQ，真实 pinned companion 配置回归进入 CI；正式原生归档解压后真实 Server/MediaMTX、readiness、全部 Web 字节和优雅退出通过；远端归档摘要与本地一致 |
| dufs-ram | 0.50.1 | [正式发布](https://github.com/isarmg/dufs-ram/releases/tag/v0.50.1)；标签级[常规 CI](https://github.com/isarmg/dufs-ram/actions/runs/33981630142)、[依赖审计](https://github.com/isarmg/dufs-ram/actions/runs/33981630132)、[独立正式签名包 E2E](https://github.com/isarmg/dufs-ram/actions/runs/33981630156) 和[发布流水线](https://github.com/isarmg/dufs-ram/actions/runs/33981630128) 全部成功；实际下载二进制校验和及版本/提交通过，附字体许可证 |
| host-monitoring | 0.8.0 | [最终 CI](https://github.com/isarmg/host-monitoring/actions/runs/33979264800) 仅 Windows 桌面失败；Linux、macOS、Server/protocol、Android/iOS 库、Web、真实 OTLP Collector 通过；不发布失败的桌面 Client |
| media-backup | 0.2.0 | [最终 CI](https://github.com/isarmg/media-backup/actions/runs/33979270192) 全部通过，含 Server 真实归档/部署、Android APK、iOS 未签名构建；正式签名密钥缺失，未发布 |
| sarmg-upgrade | 0.3.0 | [CI](https://github.com/isarmg/sarmg-upgrade/actions/runs/33978912762) 通过；当前产品 Schema 适配和正式签名仍阻断，未发布 |

发布的 Linux Server 归档不表示 Windows/macOS Client、真实摄像头、Android/iOS 应用运行或生产环境已全部验收。Sentinel HLS chunk 体积提示是构建警告，不是已关闭的验收失败。

Sentinel 0.2.0 标签及制品不覆盖，发布状态调整为预发布并注明由 0.2.1 取代：旧配置遗漏了 MediaMTX 默认启用的额外协议，先前 smoke 的环境变量掩盖了该缺口。0.2.1 的验收直接使用发行配置，只改受支持服务的测试监听地址，不覆盖 RTMP/SRT/MoQ 开关，也不提供历史升级或兼容层。

实际发布制品 SHA-256：

- Sentinel `sentinel-monitor-0.2.1-x86_64-unknown-linux-gnu.tar.gz`：`59a1fe3cba55f2597ef59fa613dc09ad390691285a9d4663bd133e2533abbe46`。
- Dufs `dufs-0.50.1-x86_64-unknown-linux-gnu`：`18fc6e5afbe9945a5fe1e5ef4d7374f07776a52885850a561ef0bec95799fa20`。GitHub 正式附件是二进制及其校验和；独立签名包 E2E 成功不表示该二进制附件另带签名包。

## 未解除的发行阻断

1. **Windows Client 文件后端**：`PrivateDirectory::create` 对目录使用普通文件打开返回 AccessDenied，Spool 无法打开；还需原生句柄、reparse-point/ACL 和持久化合同的实现及验收。不能把目录同步改为成功空操作。修复必须发布新的 Client 版本，更新消费者完整 revision，不能覆盖 0.6.0。
2. **Media Android 签名**：`android-signing` 环境需既有身份的 `MEDIA_BACKUP_ANDROID_SIGNING_PKCS12_BASE64`、`MEDIA_BACKUP_ANDROID_SIGNING_PKCS12_PASSWORD`。证书 SHA-256 必须为 `0cfc2811d48cdeab3e6d857029d879e001ab9531c06784b4d48d15a847771421`。不得另生成签名身份或替代为 debug APK。
3. **Upgrade 当前状态合同**：Host、Sunshine、Media、Sentinel 的数据库 Schema 均已变化，当前适配器尚未完成同步和真实产品验收。只改版本号不能证明可验证/备份/恢复；仍只实现当前合同，不增加历史读取器。
4. **Upgrade 签名**：需已有 `release/sarmg-upgrade-release-signing-public.pem` 对应的 `RELEASE_SIGNING_KEY_PEM`。不得换公钥、生成替代密钥或发布未签名正式包。

私钥不应粘贴到聊天或提交 Git；通过 GitHub 的 Secrets 安全配置。此次没有修改生产 systemd/安装、没有清理业务数据、没有改动第三方 `maple-font` 仓库。清理的编译缓存均可重新构建。
