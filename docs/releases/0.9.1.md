# Foundation 0.9.1

- Cargo 与 npm 依赖边界检查覆盖工作区、目标依赖、重命名依赖、覆盖声明和本地路径，拒绝从 Foundation 反向引用产品代码。
- 实例名称的浏览器输入约束与服务端契约一致，包括长度、首尾空格、控制字符和非法代理字符。
- 测试示例和文档只描述当前通用平台契约与消费者边界，不包含具体产品的业务实现。

20 个 Rust crate 和 8 个 Web package 使用统一的精确版本 `0.9.1`。本版本生成新的不可变发布资产；既有版本保持原样。
