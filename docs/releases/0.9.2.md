# Foundation 0.9.2

- 管理 Web 的实例名称校验与服务端约束一致，涵盖长度、首尾空格、控制字符和非法代理字符；登录导航回归覆盖该交互。
- 文档中的 Web 验证命令使用项目已安装的 pnpm，保证本地操作与发行门禁一致。

20 个 Rust crate 与 8 个 Web package 使用统一的精确版本 `0.9.2`。发行资产从本次不可变 tag 的源码构建和验证。
