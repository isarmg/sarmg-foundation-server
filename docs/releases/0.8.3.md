# Foundation 0.8.3

- 管理 Web 通知统一在 5 秒后自动关闭；手动关闭、退出登录和 Shell 卸载都会清理对应定时器。
- 表格列标题、数据单元格和表格内操作组统一左对齐，消除各控制平面实例列表的布局差异。
- 浏览器门禁覆盖通知自动关闭以及表头、数据和操作组的对齐合同。
- Foundation Server 继续收紧仓库所有权边界：移除 Client 专属 XML 解析组件和不属于共享 Server 基础层的同步逻辑。

这是新的不可变补丁版本。不得覆盖 `v0.8.2` 标签或资产。
