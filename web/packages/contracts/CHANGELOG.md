# Changelog

## 0.2.0

- Breaking: remove the `ApiError` type alias. Import `ErrorEnvelope` as the sole error wire type.

## 0.1.0

- Define and validate the experimental `ErrorEnvelope` wire contract.
