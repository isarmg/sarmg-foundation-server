# Changelog

## 0.2.0

- Breaking: stop re-exporting `ErrorEnvelope`; import wire contracts from `@isarmg/contracts`.

## 0.1.0

- Add bounded JSON requests, timeouts, CSRF, typed `ErrorEnvelope` failures,
  request IDs, session invalidation hooks and `Retry-After` parsing.
